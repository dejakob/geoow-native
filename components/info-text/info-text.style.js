import { createStyle } from 'react-native-styler';

createStyle({
    infoText: {
        padding: '10w4s',
        color: 'theme:subtext',
        fontSize: 'theme:small',
        textAlign: 'center'
    }
});
