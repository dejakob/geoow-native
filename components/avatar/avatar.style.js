import { createStyle } from 'react-native-styler';

createStyle({
    avatar: {
        height: '100h4s',
        width: '100h4s',
        borderRadius: '50h4s',
        backgroundColor: 'theme:primary',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '3h4s'
    }
});
