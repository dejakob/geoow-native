import { createStyle } from 'react-native-styler';

createStyle({
    publicBackground: {
        container: {
            flex: 1,
        },
        image: {
            height: '100vh',
            width: '100vw'
        }
    }
});
