import { createStyle } from 'react-native-styler';

createStyle({
    card: {
        flex: 1,
        backgroundColor: 'theme:sheet',
        borderRadius: '2h4s',
        marginTop: '20h4s',
        padding: '10w4s',
        minHeight: '100h4s',
        justifyContent: 'center',
        alignItems: 'center'
    }
});
