import { createStyle } from 'react-native-styler';

createStyle({
    auth: {
        title: {
            color: 'theme:primary',
            fontSize: 'theme:h1',
            fontWeight: '100',
            textAlign: 'center',
            marginBottom: '30h4s'
        }
    }
});
