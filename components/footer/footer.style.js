import { createStyle } from 'react-native-styler';

createStyle({
    footer: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        margin: '20w4s',
        height: '40h4s'
    }
});
