import { createStyle } from 'react-native-styler';

createStyle({
    authVerify: {
        flex: 1,
        justifyContent: 'center',

        input: {
            marginTop: '30h4s'
        }
    }
});
